general {
        colors = true
        interval = 5
}

order += "battery all"
order += "disk /"
order += "tztime local"

battery all {
        format = "%status %percentage %remaining"
}

disk "/" {
        format = "%avail"
}

tztime local {
        format = "%Y-%m-%d %H:%M:%S"
}
